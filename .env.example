# 示例环境变量（开发/部署参考）
#
# 默认情况下：如果不设置 PASSWORD，将会被重定向到 /warning。
# 开发环境调试时：可以通过 DEV_ALLOW_EMPTY_PASSWORD=true 临时绕过（仅开发环境生效）。

# 站长账号（仅非 localstorage 存储需要）
# USERNAME=admin

# 访问密码（生产环境建议必填；示例值仅用于本地开发，建议你自行修改为强密码）
# PASSWORD=moontv_dev_password

# 开发调试：允许不设置 PASSWORD 直接访问（仅开发环境生效）
# DEV_ALLOW_EMPTY_PASSWORD=true

# 站点信息
SITE_NAME=MoonTV
ANNOUNCEMENT=本网站仅提供影视信息搜索服务，所有内容均来自第三方网站。本站不存储任何视频资源，不对任何内容的准确性、合法性、完整性负责。

# 存储类型：localstorage / redis / d1 / upstash
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# 功能开关
NEXT_PUBLIC_ENABLE_REGISTER=false
NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false

# 搜索
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 代理（可选）
NEXT_PUBLIC_IMAGE_PROXY=
NEXT_PUBLIC_DOUBAN_PROXY=

# TMDB API Key（从 https://www.themoviedb.org/settings/api 获取）
TMDB_API_KEY=

# Redis / Upstash（当 NEXT_PUBLIC_STORAGE_TYPE=redis/upstash 时使用）
# REDIS_URL=redis://localhost:6379
# UPSTASH_URL=
# UPSTASH_TOKEN=
